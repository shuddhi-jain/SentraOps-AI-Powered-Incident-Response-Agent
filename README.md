# SentraOps-AI-Powered-Incident-Response-Agent
docker-compose.yml services:
- redis (official redis:alpine image — free)
- api (.NET Core)
- agent (Python/LangChain)
- dashboard (Angular, served separately or via nginx container)
